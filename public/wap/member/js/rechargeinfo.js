$(function() {    var e = getCookie("key");    if (!e) {        window.location.href = WapSiteUrl + "/member/login.html";        return    }    var paysn = getQueryString('paysn');    $.ajax({        type: 'post',        url: ApiUrl + "/Recharge/recharge_order.html",        data: {key: e, paysn: paysn},        dataType: 'json',        success: function(result) {            if (result.code == 10000) {                var data = result.result;                data.ApiUrl = ApiUrl;                data.key = e;                template.helper('p2f', function(s) {                    return (parseFloat(s) || 0).toFixed(2);                });                var html = template('rechargeinfo-tmpl', data);                $("#rechargeinfo").html(html);            }else {                alert(result.message);                location.href = "member.html?act=member";            }        }    });});