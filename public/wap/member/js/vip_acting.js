//会员激活$(function () {    var e = getCookie("key");    if (!e) {        window.location.href = WapSiteUrl + "/member/login.html";        return;    }    $.sValid.init({        rules: {            pdr_amount: "required"        },        messages: {            pdr_amount: "请输入激活码！"        },        callback: function (e, r, a) {            if (e.length > 0) {                var c = "";                $.map(r, function (e, r) {                    c += "<p>" + e + "</p>";                });                layer.open({content: c, skin: 'msg', time: 2});            }        }    });    $("#saveform").click(function () {        if (!$(this).parent().hasClass("ok")) {            return false;        }        if ($.sValid()) {            var card_no = $.trim($('#pdr_amount').val());            var key = e;            if (!key) {                window.location.href = WapSiteUrl + "/member/login.html";                return;            }            //var client = 'wap';            /* var r = $("#rc_sn").val();             var a = $.trim($("#captcha").val());             var c = $.trim($("#codekey").val()); */            $.ajax({                type: "post",                url: ApiUrl + "/plus/activation.html",                data: {card_no: card_no, key: key},                dataType: "json",                success: function (result) {                    if (result.code == 10000) {                        layer.open({                            content: '恭喜您，激活成功！'                            , btn: ['确定']                            , yes: function (index) {                                layer.close(index);                                location.href = 'member.html';                            }                        });                    } else {                        layer.open({content: result.message, skin: 'msg', time: 2});                    }                }            });        }    });});